<circle
    {% if circle.tag %} 
        class="{{ circle.tag }}"
    {% endif %} 
    {% if circle.clip_id %}
    clip-path="url(#{{ circle.clip_id }})"
    {% endif %}
     transform="
        translate({{ circle.x }} {{  circle.y }}) 
        scale({{ circle.scale.x }} {{ circle.scale.y }})
        rotate({{ circle.rotate }})
    " 
    cx="0" 
    cy="0" 
    r="{{ circle.r }}"
/>