<g 
    {% if tags %}
        class="{{ tags }}" 
    {% endif %}
    transform="
        translate({{ x * scale.x }} {{  y * scale.y }})
    "
>
    {% block element %}
        {{ content|safe }}
    {% endblock %}
</g>