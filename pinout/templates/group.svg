<g 
    {% if group.tag %}
        class="{{ group.tag }}"
    {% endif %}
    transform="
        translate({{ group.x }} {{  group.y }}) 
        scale({{ group.scale.x }} {{ group.scale.y }})
    "
>
    {% block content %}
        {{ group.render_children()|safe }}
    {% endblock %}
</g>