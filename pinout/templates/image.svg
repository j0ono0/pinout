<image 
    {% with params=image %}
        {% include "component_common.svg" %}
    {% endwith %}
    
    href="{{ image.src }}" 
    xlink:href="{{ image.src }}" 
    height="{{ image.height }}" 
    width="{{ image.width }}"
/>