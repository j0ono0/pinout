<rect

    {% with params=rect %}
        {% include "component_common.svg" %}
    {% endwith %}
    
    height="{{ rect.height }}" 
    width="{{ rect.width }}"
    {% if rect.corner_radius %}
    rx="{{ rect.corner_radius }}" 
    ry="{{ rect.corner_radius }}" 
    {% endif %}
/>