<text
    {% if text.tag %}
    class="{{ text.tag }}"
    {% endif %}
    transform="
        translate({{ text.x }} {{  text.y }}) 
        scale({{ text.scale.x }} {{ text.scale.y }})
    "  
>
    {{ text.content }}
</text>
