
<text
    transform="scale({{ scale.x }} {{ scale.y }})" 
    x="0"
    y="{{ y * scale.y - line_height }}" 
>
    {% for line in text %}
    <tspan x="{{ x * scale.x }}" dy="{{ line_height }}"> {{ line }}</tspan>
    {% endfor %}
</text>

