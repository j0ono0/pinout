<use
    id="{{ use.id }}"
    {% if use.tag %} 
        class="{{ use.tag }}"
    {% endif %} 
    {% if use.clip %}
        clip-path="url(#{{ use.clip.id }})"
    {% endif %}
    
     transform="
        translate({{ use.x }} {{  use.y }}) 
        scale({{ use.scale.x }} {{ use.scale.y }})
        rotate({{ use.rotate }})
    " 

    href="#{{ use.target_id }}"
    
/>

